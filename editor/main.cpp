import std;
import Window;
import Math;
import Graphics.Shader;
import Graphics.Render.Drawable.Triangle;
import Debug.Logger;
import Event;

auto main() -> int {
    // try {
    // 	const Window window(640, 480, "Hello World");
    // 	window.loop();
    // } catch (const std::exception& e) {
    // 	Logger::error(e.what());
    // 	return -1;
    // }
    return 0;
}
